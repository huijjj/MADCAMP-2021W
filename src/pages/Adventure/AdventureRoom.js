import { useLocation, useNavigate, useParams } from "react-router-dom";

export default function AdventureRoom() {
    const thisFloor = useParams();

    function getRandomRoom() {
        // const ran =
    }


    return (
      <div>
        {/* {(getRandomRoom() === 0)
        ? } */}
      </div>
    );
  }